package model.enums;

/**
 * An enumeration representing the different types of greyscales that can be applied to an image.
 */
public enum GreyscaleComponentType { RED, GREEN, BLUE, VALUE, INTENSITY, LUMA }
